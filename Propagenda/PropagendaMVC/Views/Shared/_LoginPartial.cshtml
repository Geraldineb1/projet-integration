    <ul class="navbar-nav">
        @if (User.Identity.IsAuthenticated)
                        {
                            @if (!User.IsInRole("Provider"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link " title="Manage" asp-controller="Providers" asp-action="Create">
                                        DEVENIR PRESTATAIRE
                                    </a>
                                </li>
                            }
                            @*@if (User.IsInRole("Administrator") || User.IsInRole("Employee") || User.IsInRole("Provider"))
                            {*@
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown"aria-haspopup="true" aria-expanded="false">
                                        Gérer
                                    </a>
                                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                        @if(User.IsInRole("Administrator") || User.IsInRole("Employee"))
                                        {
                                            <a class="nav-link text-dark" asp-area="" asp-controller="ServiceTypes" asp-action="Index">Type de service</a>
                                            <a class="nav-link text-dark" asp-area="" asp-controller="TicketReservations" asp-action="Index">Ticket Reservations</a>
                                            <a class="nav-link text-dark" asp-area="" asp-controller="Services" asp-action="ServicesToApprove">Approuver services</a>
                                            <a class="nav-link text-dark" asp-area="" asp-controller="Providers" asp-action="ProvidersToApprove">Approuver prestataires</a>
                                            <a class="nav-link text-dark" asp-area="" asp-controller="Events" asp-action="EventsToApprove">Approuver événements</a>
                                        }
                                        @if(User.IsInRole("Provider"))
                                        {
                                            <a class="nav-link text-dark" asp-area="" asp-controller="Providers" asp-action="ProvidersByUser">Mes prestataires</a>
                                            <a class="nav-link text-dark" asp-area="" asp-controller="Services" asp-action="ServicesByUser">Mes services</a>
                                        }
                                        <a class="nav-link text-dark" asp-area="" asp-controller="Events" asp-action="EventsByUser">Mes événements</a>
                                        <a class="nav-link text-dark" asp-area="" asp-controller="ServiceReservations" asp-action="ServiceReservationsByUser">Demandes de service</a>
                                        
                                    </div>
                                </li>
                                 
                                
                           @* }*@
                        }
        @if (User.Identity.IsAuthenticated)
        {
            <li class="nav-item">
                <a class="nav-link " href="#" title="Manage">
                    @User.Identity.Name!
                </a>
            </li>
            <li class="nav-item">
                <form class="form-inline" method="post" asp-action="Logout" asp-controller="Users" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link ">
                        Logout
                    </button>
                </form>
            </li>
        }
        else
        {
            <li class="nav-item">

                <a class="nav-link" asp-action="Register" asp-controller="Users">
                    S'inscrire
                </a>
            </li>
            <li class="nav-item">

                <a class="nav-link" asp-action="Login" asp-controller="Users">
                    Se connecter
                </a>
            </li>
        }
    </ul>
